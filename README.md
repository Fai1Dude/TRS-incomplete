### this is same as train reservation system project but built with Vite React + Tailwind CSS, Also Node.js as Backend and MySQL as DB 
### (( only saved to show the Frontend Work with some funcations Wokring ))
